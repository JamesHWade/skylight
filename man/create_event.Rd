% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_calendar.R
\name{create_event}
\alias{create_event}
\title{Create a New Calendar Event}
\usage{
create_event(
  title,
  start,
  end = NULL,
  calendar_id = "primary",
  description = NULL,
  location = NULL,
  all_day = NULL
)
}
\arguments{
\item{title}{Character. Event title/summary.}

\item{start}{POSIXct or Date. Event start time. If Date, creates an all-day event.}

\item{end}{POSIXct or Date. Event end time. Defaults to 1 hour after start for
timed events, or next day for all-day events.}

\item{calendar_id}{Character. Calendar to add the event to.
Defaults to "primary" (user's main calendar).}

\item{description}{Character. Optional event description.}

\item{location}{Character. Optional event location.}

\item{all_day}{Logical. Force all-day event. If NULL, determined by \code{start} class.}
}
\value{
A list with \code{success} (logical), \code{event} (created event data), and
\code{error} (error message if failed).
}
\description{
Creates an event on Google Calendar using the Events.insert API.
}
\examples{
if (interactive() && is_authenticated()) {
  # Create a 1-hour event
  result <- create_event(
    title = "Team Meeting",
    start = Sys.time() + 3600,
    calendar_id = "primary"
  )

  # Create an all-day event
  result <- create_event(
    title = "Vacation",
    start = Sys.Date() + 7,
    end = Sys.Date() + 14
  )
}
}
